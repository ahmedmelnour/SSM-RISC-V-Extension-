# Measurement

Stage 5. Method, counters used, and results, so a number can always be traced
back to the build that produced it (`fw/build_info.txt` records program, opt
level, `-march` and compiler version).

Not started.
